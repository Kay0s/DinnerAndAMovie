<div class="container content">

  <h1>This is all your history section</h1> 

  <br>

  <div class="row">
    <div class="col-lg-12 bg-warning text-center">
      <h3>Previous Movies</h3>
    
          <button class=" submit delete-favorite ">Delete</button>
      </form>
    </div>
  </div>

  <div class="container m-3 p-3">
    <div class="row">
      <div class="col-md-6">
        <h2> Favorite Movies</h2>
        <div class="=content-dinners">
          {{!-- <ul> --}}
          {{#each dinner}}
          {{#unless likes}}
          {{> dinner/history-block devoured=true}}
          {{/unless}}
          {{/each}}
        </div>
        {{!-- </ul> --}}
      </div>

      <div class="col-md-6">
        <h2>Favorites Dinner and Recipes</h2>

        {{!-- <ul> --}}
        {{#each dinner}}
        {{#if likes}}
        {{> dinner/history-block devoured=false}}
        {{/if}}
        {{/each}}
        {{!-- </ul> --}}
      </div>
    </div>
  </div>
</div>
</div>